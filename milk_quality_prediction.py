# -*- coding: utf-8 -*-
"""Milk_Quality_Prediction.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1d5BeAAHtwGnphpRbmlCFeQuXCAtywyai
"""



# read the dataset
df = pd.read_csv('Milk_data.csv')

x=df.iloc[ : , : -1]
y=df.iloc[ : , -1 ]
print(x)
print(y)

from sklearn.model_selection import train_test_split
x_train, x_test, y_train, y_test = train_test_split(x, y,test_size=0.25,random_state=0)
print(x_train)
print(y_train)
print(x_test)
print(y_test)

from sklearn.preprocessing import StandardScaler
st_x = StandardScaler()
x_train = st_x.fit_transform(x_train)
x_test = st_x.transform(x_test)

from sklearn.tree import DecisionTreeClassifier
classifier = DecisionTreeClassifier(criterion ='entropy',random_state=0)
classifier.fit(x_train,y_train)

#for coln in df:
  #df[coln]=df[coln].astype(category).cat.codes

y_pred=classifier.predict(x_test)
plt.plot(y_pred)
plt.show

from sklearn.metrics import confusion_matrix
y_pred = classifier.predict(x_test)
cm = confusion_matrix(y_test,y_pred)
plt.plot(cm)
plt.show

from sklearn.tree import export_graphviz
import graphviz 
graphviz.Source(export_graphviz(classifier,feature_names=x.columns, filled=True, class_names=['1','2','3','4','5','6','7'], out_file=None))